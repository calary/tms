module.exports = {
  name: 'selectTags',
  template('./selectTags.type.html'),
  controller: ['$scope', '$q', function($scope, $q){
    var model = $scope.key;
    var key = $scope.options.key;

    

  }]
};